#include <stdio.h>

int main(){
	// int sign = 1,max;
	// double deno = 2.0,sum = 1.0,term;
	// printf("输入最大值：");
	// scanf("%d",&max);
	// while (deno <= max){
	// 	term = sign / deno;
	// 	sum -= term;
	// 	deno = deno + 1;
	// }

	// printf("值为:%f\n", sum);

	// unsigned short a;
	// printf("输入a的值:");
	// scanf("%hd",&a);
	// printf("%d\n", a);

	unsigned int a;
	printf("输入a的值:");
	scanf("%d",&a);
	printf("%d\n", a);
	return 0;
}